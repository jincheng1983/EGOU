# 程序集 stringx
// 示例扩展包：字符串扩展函数。位于 IDE 同级 libs/stringx/，所有项目共享。

// 反转文本：把文本字符顺序反转。
// 示例：反转文本("abc") → "cba"
函数 反转文本(原始文本 文本型) 文本型
    局部变量 结果, 文本型
    结果 = ""
    循环 字符 ＝ 范围 原始文本
        结果 = 文本型(字符) + 结果
    结束循环
    返回 结果
结束函数

// 重复文本：把文本重复 N 次。
// 示例：重复文本("ab", 3) → "ababab"
函数 重复文本(原始文本 文本型, 次数 整数型) 文本型
    局部变量 结果, 文本型
    结果 = ""
    循环 i := 0; i < 次数; i++
        结果 = 结果 + 原始文本
    结束循环
    返回 结果
结束函数
